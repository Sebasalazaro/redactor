{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Both windows only talk to the app's own commands and events. No plugin (clipboard, shortcuts, fs, http) is exposed to the webview: those run in Rust.",
  "windows": ["review", "dashboard"],
  "permissions": ["core:event:default"]
}
